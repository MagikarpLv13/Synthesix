{
  "name": "synthesix-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synthesix UI components — Palier 1 (TypeScript + Lit Web Components).",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
